# macOS
.DS_Store

# Xcode user state / local settings
xcuserdata/
*.xcuserstate
*.xcscmblueprint

# Build products
DerivedData/
build/
*.ipa
*.dSYM.zip
*.dSYM

# SwiftPM
.build/
.swiftpm/

# Tooling caches
*.profraw
*.log

# Note: HWANGTODO.xcodeproj is generated by `make gen` (XcodeGen, project.yml
# is the source of truth). It stays tracked for now so plain `xcodebuild`
# works on a fresh clone — regenerate after editing project.yml.
